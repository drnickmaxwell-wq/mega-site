import Placeholder from "@/components/shell/Placeholder";
export const metadata = { title: "Preview – Light" };
export default function Page() {
  return <Placeholder title="Light Theme Preview" intro="Theme shell preview (design polish later)." />;
}

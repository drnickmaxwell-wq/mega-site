import Placeholder from "@/components/shell/Placeholder";
export const metadata = { title: "Preview – Ink" };
export default function Page() {
  return <Placeholder title="Ink Theme Preview" intro="Theme shell preview (design polish later)." />;
}

import Placeholder from "@/components/shell/Placeholder";
export const metadata = { title: "Aligner Journey | St Mary’s House Dental Care" };
export default function Page() {
  return <Placeholder title="Aligner Journey" intro="Photos → scan → aligners → monitoring → retainers." />;
}

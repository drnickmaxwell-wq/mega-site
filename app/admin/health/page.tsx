import Placeholder from "@/components/shell/Placeholder";
export const metadata = { title: "Admin – Health" };
export default function Page() {
  return <Placeholder title="Admin – Health" intro="Basic health checks (dev only)." />;
}
